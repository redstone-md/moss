name: Release Main

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: release-main-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-shared:
    name: Build Shared (${{ matrix.label }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            label: linux-amd64
            output: libmoss.so
            header: libmoss.h
            goos: linux
            goarch: amd64
          - os: windows-latest
            label: windows-amd64
            output: moss.dll
            header: moss.h
            goos: windows
            goarch: amd64
          - os: macos-15-intel
            label: macos-amd64
            output: libmoss.dylib
            header: libmoss.h
            goos: darwin
            goarch: amd64
          - os: macos-latest
            label: macos-arm64
            output: libmoss.dylib
            header: libmoss.h
            goos: darwin
            goarch: arm64
    env:
      CGO_ENABLED: "1"
      GOOS: ${{ matrix.goos }}
      GOARCH: ${{ matrix.goarch }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup MinGW
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@e9898307ac31d1a803454791be09ab9973336e1c # v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc

      - name: Build shared library
        run: go build -buildmode=c-shared -o ${{ matrix.output }} ./cmd/moss-ffi

      - name: Package shared artifact
        shell: pwsh
        run: |
          $archive = "moss-${{ matrix.label }}-shared.zip"
          Compress-Archive -Path "${{ matrix.output }}", "${{ matrix.header }}" -DestinationPath $archive -Force

      - name: Upload shared artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: moss-${{ matrix.label }}-shared
          path: moss-${{ matrix.label }}-shared.zip
          if-no-files-found: error

  publish-release:
    name: Publish GitHub Release
    needs: build-shared
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          # Full history and tags: the notes are built from the commits between
          # this tag and the last one, and a shallow clone has neither.
          fetch-depth: 0

      - name: Download shared artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          pattern: moss-*-shared
          path: release-assets
          merge-multiple: true

      - name: Build release notes
        run: bash scripts/release-notes.sh "$GITHUB_REF_NAME" > release-notes.md

      - name: Publish release assets
        env:
          GH_REPO: ${{ github.repository }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          # --generate-notes is deliberately not used: it builds its list from
          # merged PULL REQUESTS, and this repo commits straight to main. There
          # were none to list, so every release after v0.7.2 got a body of one
          # compare link and nothing else. release-notes.sh uses the changelog
          # section plus the actual commits instead.
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" release-assets/*.zip --clobber
            gh release edit "$GITHUB_REF_NAME" --notes-file release-notes.md
          else
            gh release create "$GITHUB_REF_NAME" release-assets/*.zip \
              --title "$GITHUB_REF_NAME" --notes-file release-notes.md
          fi
